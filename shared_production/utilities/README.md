# Shared Utilities

- `build_coco_obb.py`: builds the existing COCO-OBB dataset representation.
- `generate_prompt_comparison.py`: compares the existing seeded prompt builders.

Worker-specific workflow mutation remains inside each worker.
