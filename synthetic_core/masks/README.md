# Existing Mask Assets

This directory contains copies of existing binary meter/background masks only.

- White pixels are the editable background region.
- Black pixels are the protected meter/object region.
- The same binary image describes both complementary regions.

No digit-reader, individual-digit, or wheel masks are included or generated.
